const compareStats = () => {
	const listItem = document.querySelector('.attribute-selector');

	if (listItem.value) {
		let champion = null;
		let statToCompare = listItem.value;

		console.log(statToCompare);
	}
};

export default compareStats;
